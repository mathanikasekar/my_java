package collections;
import java.lang.reflect.Array;
import java.util.*;

public class LinkedLists extends Stack {
    public static void main(String[] args)
    {
        LinkedList ll = new LinkedList();
        ll.add(5);
        ll.add(5.5);
        ll.add("deva");
        Object[] ob=ll.toArray(new Object[ll.size()]);
        for(int i=0;i<ll.size();i++)
        {
            System.out.println(ob[i]);
        }
    }
}
